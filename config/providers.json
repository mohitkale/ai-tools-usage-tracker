{
  "schema_version": 1,
  "providers": [
    {
      "id": "claude",
      "display_name": "Claude Code",
      "stability": "stable",
      "enabled_by_default": false,
      "permissions": ["local_metadata", "local_payload"],
      "credential_access": "none",
      "network_hosts": [],
      "discovery": {
        "executables": ["claude"],
        "paths": []
      },
      "notes": "Consumes official status-line JSON supplied over stdin; it does not read Claude credentials."
    },
    {
      "id": "codex",
      "display_name": "Codex",
      "stability": "experimental",
      "enabled_by_default": false,
      "permissions": ["local_metadata", "local_process", "delegated_provider_network"],
      "credential_access": "delegated_to_official_process",
      "network_hosts": [],
      "discovery": {
        "executables": ["codex"],
        "paths": []
      },
      "notes": "Uses the official local Codex app-server over stdio; never reads auth.json."
    },
    {
      "id": "github_copilot",
      "display_name": "GitHub Copilot",
      "stability": "experimental",
      "enabled_by_default": false,
      "permissions": ["local_metadata", "local_process", "delegated_provider_network"],
      "credential_access": "delegated_to_official_process",
      "network_hosts": [],
      "discovery": {
        "executables": ["gh"],
        "paths": []
      },
      "notes": "Uses the official GitHub CLI to call the personal premium-request billing endpoint; the app never reads or stores gh credentials."
    },
    {
      "id": "devin",
      "display_name": "Devin",
      "stability": "experimental",
      "enabled_by_default": false,
      "permissions": ["local_metadata", "local_payload"],
      "credential_access": "none",
      "network_hosts": [],
      "discovery": {
        "executables": [],
        "paths": []
      },
      "notes": "Reads only windsurf.settings.cachedPlanInfo from Devin's local state database in read-only mode; auth and session records are never selected."
    },
    {
      "id": "cursor",
      "display_name": "Cursor",
      "stability": "private_experimental",
      "enabled_by_default": false,
      "permissions": ["local_metadata", "private_state", "provider_network"],
      "credential_access": "existing_session_opt_in_only",
      "network_hosts": ["api2.cursor.sh"],
      "discovery": {
        "executables": ["cursor"],
        "paths": []
      },
      "notes": "Explicit opt-in reads only cursorAuth/accessToken from Cursor/User/globalStorage/state.vscdb in read-only mode, calls only the official api2.cursor.sh usage RPC, and retains only normalized quota fields."
    },
    {
      "id": "antigravity",
      "display_name": "Antigravity",
      "stability": "experimental",
      "enabled_by_default": false,
      "permissions": ["local_metadata", "local_payload"],
      "credential_access": "none",
      "network_hosts": [],
      "discovery": {
        "executables": ["antigravity"],
        "paths": []
      },
      "notes": "Reads only antigravityUnifiedStateSync.modelCredits from the local state database in read-only mode; the OAuth record is never selected."
    }
  ]
}
